-- ============================================================
-- V7: 채널 목록 조회 성능 보강
-- channel summary indexes
-- ============================================================


-- ------------------------------------------------------------
-- threads
-- 채널 목록 요약 조회에서 channel_id, thread_type으로 필터링하고
-- created_at, id 기준으로 채널별 마지막 메시지를 찾기 위한 인덱스
-- ------------------------------------------------------------
CREATE INDEX idx_threads_ch_type_ca_id
    ON threads (channel_id, thread_type, created_at, id);
